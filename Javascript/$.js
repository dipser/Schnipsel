window.$q = document.querySelector.bind(document); // Same as $(selector, [startNode]) in modern browser commandline
window.$all = document.querySelectorAll.bind(document); // Same as $$(selector, [startNode]) in modern browser commandline

// The dollar sign should only be used in frameworks.
